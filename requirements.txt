# ----------------------------------------------------------------------------
# Pearls AQI Predictor - Python dependencies
# Install with:  pip install -r requirements.txt
# ----------------------------------------------------------------------------

# --- Core data / ML -----------------------------------------------------
numpy>=1.26,<2.2
pandas>=2.1,<2.3
scikit-learn>=1.4,<1.6
xgboost>=2.0,<2.2
joblib>=1.3,<1.5
pyarrow>=14.0,<17.0          # parquet engine used by the local feature store

# --- HTTP / retries -------------------------------------------------------
requests>=2.31,<3.0
tenacity>=8.2,<9.0           # robust retry/backoff for API calls
python-dotenv>=1.0,<2.0

# --- Explainability ---------------------------------------------------------
shap>=0.45,<0.47
matplotlib>=3.8,<3.10

# --- Web app -----------------------------------------------------------
streamlit>=1.35,<1.39
plotly>=5.20,<5.25

# --- Testing / quality --------------------------------------------------
pytest>=8.0,<9.0
pytest-mock>=3.14,<4.0
responses>=0.25,<0.26         # mocked HTTP responses for API-client tests

# --- Optional: deep-learning forecaster --------------------------------
# TensorFlow is heavy and not required for the app to run. The training
# pipeline automatically skips the neural-network candidate if this is not
# installed. Uncomment to enable it:
# tensorflow>=2.16,<2.18

# --- Optional: managed feature store / model registry -------------------
# Only needed if HOPSWORKS_API_KEY is set in your environment (see .env.example).
# hopsworks>=4.0,<5.0

# --- Optional: running notebooks/01_eda.ipynb ---------------------------
# jupyterlab>=4.0
# ipykernel>=6.29
